import React from "react";
import { storiesOf } from "@storybook/react";
import { action } from "@storybook/addon-actions";
import Controls from ".";

storiesOf("Controls", module).add("Default", () => <Controls />);
